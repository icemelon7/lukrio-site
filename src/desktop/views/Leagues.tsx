import React, { FunctionComponent } from 'react';

type LeaguesProps = {
    moreLink?: boolean;
}

const Leagues : FunctionComponent<LeaguesProps> = ({moreLink}) => {
    
    return (
        <div className="leagues">
            
        </div>
    )
}

export default Leagues;